diff --git a/./yajsw-stable-13.11/src/yajsw/src/main/java/org/rzo/yajsw/log/MyFileHandler.java b/yajsw-stable-13.11_patched/src/yajsw/src/main/java/org/rzo/yajsw/log/MyFileHandler.java
index bd59bf7..d8d4013 100644
--- a/./yajsw-stable-13.11/src/yajsw/src/main/java/org/rzo/yajsw/log/MyFileHandler.java
+++ b/yajsw-stable-13.11_patched/src/yajsw/src/main/java/org/rzo/yajsw/log/MyFileHandler.java
@@ -177,7 +177,11 @@ public class MyFileHandler extends StreamHandler
 	public MyFileHandler() throws IOException
 	{
 		init(null, null, null, null);
-		System.out.println("MyFilleHandler0 "+umask);
+		/*
+                 * Removed extraneous logging.
+                 * lsingh
+                 */
+		//System.out.println("MyFilleHandler0 "+umask);
 
 	}
 
@@ -196,7 +200,12 @@ public class MyFileHandler extends StreamHandler
 	{
 
 		int prevUmask = -1;
-		System.out.println("initOutputFiles: "+umask);
+		/*
+		 * Removed extraneous logging.
+		 * lsingh 
+		 */
+		//System.out.println("initOutputFiles: "+umask);
+	
 		if (umask != -1)
 			prevUmask = OperatingSystem.instance().processManagerInstance()
 					.umask(umask);
@@ -404,7 +413,12 @@ public class MyFileHandler extends StreamHandler
 			len = (int) fname.length();
 		}
 		int prevUmask = -1;
-		System.out.println("open umask "+umask);
+
+		/*
+                 * Removed extraneous logging.
+                 * lsingh
+                 */
+		//System.out.println("open umask "+umask);
 		if (umask != -1)
 			prevUmask = OperatingSystem.instance().processManagerInstance()
 					.umask(umask);
@@ -685,7 +699,11 @@ public class MyFileHandler extends StreamHandler
 		}
 		init(pattern, null, Integer.valueOf(DEFAULT_LIMIT),
 				Integer.valueOf(DEFAULT_COUNT));
-		System.out.println("MyFilleHandler6 "+umask);
+		/*
+                 * Removed extraneous logging.
+                 * lsingh 
+                 */
+		//System.out.println("MyFilleHandler6 "+umask);
 
 	}
 
@@ -716,7 +734,11 @@ public class MyFileHandler extends StreamHandler
 		}
 		init(pattern, Boolean.valueOf(append), Integer.valueOf(DEFAULT_LIMIT),
 				Integer.valueOf(DEFAULT_COUNT));
-		System.out.println("MyFilleHandler1 "+umask);
+		/*
+                 * Removed extraneous logging.
+                 * lsingh 
+                 */
+		//System.out.println("MyFilleHandler1 "+umask);
 
 	}
 
@@ -755,7 +777,11 @@ public class MyFileHandler extends StreamHandler
 			throw new IllegalArgumentException("limit < 0 || count < 1");
 		}
 		init(pattern, null, Integer.valueOf(limit), Integer.valueOf(count));
-		System.out.println("MyFilleHandler5 "+umask);
+		/*
+                 * Removed extraneous logging.
+                 * lsingh 
+                 */
+		//System.out.println("MyFilleHandler5 "+umask);
 
 	}
 
@@ -800,7 +826,11 @@ public class MyFileHandler extends StreamHandler
 		}
 		init(pattern, Boolean.valueOf(append), Integer.valueOf(limit),
 				Integer.valueOf(count));
-		System.out.println("MyFilleHandler3 "+umask);
+		/*
+                 * Removed extraneous logging.
+                 * lsingh 
+                 */
+		//System.out.println("MyFilleHandler3 "+umask);
 
 	}
 
@@ -809,7 +839,11 @@ public class MyFileHandler extends StreamHandler
 	{
 		this(pattern, limit, count, append, compress, umask);
 		this.desc = desc;
-		System.out.println("MyFilleHandler2 "+umask);
+		/*
+                 * Removed extraneous logging.
+                 * lsingh
+                 */
+		//System.out.println("MyFilleHandler2 "+umask);
 	}
 
 	public MyFileHandler(String pattern, int limit, int count, boolean append,
